import React from "react";
import { View, Text } from "react-native";

const RecipieFeed: React.FC = (props) => {
    return (
        <View style={{ justifyContent: "center", alignItems: "center" }}>
            <Text>Recipie Screen</Text>
        </View>
    );
}

export default RecipieFeed;